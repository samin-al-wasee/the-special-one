name: ts1_persistence
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  drift: ^2.32.1
  path: ^1.9.1

dev_dependencies:
  build_runner: ^2.13.1
  drift_dev: ^2.32.1
  lints: ^6.0.0
  test: ^1.25.6
